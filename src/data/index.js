const bowlTypes = require('./bowlTypes')
const shotTypes = require('./shotTypes')
const shotTimings = require('./shotTimings')
const teams = require('./teams')
const outcomes = require('./outcomes')

module.exports = {
  bowlTypes,
  shotTypes,
  shotTimings,
  teams,
  outcomes
}
